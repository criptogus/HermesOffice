node_modules
out/
dist/
release/
*.log
.DS_Store
.refs/
.env
.env.*
electron-builder.env
# Local OAuth client credentials for the embedded Drive sync (never committed)
packages/hermes-cloud/src/oauth-credentials.ts
~$*

# Local ad-hoc e2e driver scripts (not pushed); the folder and README are tracked, the scripts are not
/scripts/drivers/driver.*.mjs
/probe*.mjs
/apps/shell/build/THIRD-PARTY-NOTICES.txt
# generated at build time with the local HEAD commit (never version it — it
# always diverges from origin/main the moment a new commit lands)
/apps/shell/build/build-info.json
/example/
/.task/
/docs-archive/
.idea/
.claude/

# Local build artifacts dropped in the repo root
/*.dmg
/*.exe
/*.zip
/windows/

.cursor/rules/git-remote.mdc
__pycache__/
/notes/
/MEMORY.md
